[package]
name = "lucide"
version = "0.1.0"
category = "icons"
description = "Spec wrappers for the Lucide icon set"
license = "MIT"

[compatibility]
spec = ">=0.1.0"

[source]
origin = "@spec/icons-lucide"
