[package]
name = "skeleton"
version = "0.1.0"
category = "components"
description = "Animated placeholder shapes displayed while content is loading"
license = "MIT"

[compatibility]
spec = ">=0.1.0"

[source]
origin = "@spec/components"
file = "skeleton.spec"
