[package]
name = "text-input"
version = "0.3.0"
category = "components"
description = "Single-line text field with label, icon, segmented unit, and validation support"
license = "MIT"

[compatibility]
spec = ">=0.1.0"

[source]
origin = "@spec/components"
file = "text-input.spec"
