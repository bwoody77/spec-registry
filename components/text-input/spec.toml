[package]
name = "text-input"
version = "0.7.2"
category = "components"
description = "Single-line text field with label, leading/trailing icon, segmented unit, tone (warning/danger/highlight), and validation support"
license = "MIT"

[compatibility]
spec = ">=0.1.0"

[source]
origin = "@spec/components"
file = "text-input.spec"
