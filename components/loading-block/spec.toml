[package]
name = "loading-block"
version = "0.2.0"
category = "components"
description = "Centered spinner with label for loading sections"
license = "MIT"

[compatibility]
spec = ">=0.1.0"

[source]
origin = "@spec/components"
file = "loading-block.spec"
