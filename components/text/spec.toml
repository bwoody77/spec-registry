[package]
name = "text"
version = "0.1.0"
category = "components"
description = "Typography primitive for rendering styled text with semantic variants"
license = "MIT"

[compatibility]
spec = ">=0.1.0"

[source]
origin = "@spec/components"
file = "text.spec"
