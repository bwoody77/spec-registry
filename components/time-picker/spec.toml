[package]
name = "time-picker"
version = "0.1.0"
category = "components"
description = "Input control for selecting a time value with hour and minute fields"
license = "MIT"

[compatibility]
spec = ">=0.1.0"

[source]
origin = "@spec/components"
file = "time-picker.spec"
