[package]
name = "select"
version = "0.1.0"
category = "components"
description = "Dropdown menu for choosing a single option from a list"
license = "MIT"

[compatibility]
spec = ">=0.1.0"

[source]
origin = "@spec/components"
file = "select.spec"
