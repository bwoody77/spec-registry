[package]
name = "slider"
version = "0.1.0"
category = "components"
description = "Draggable range input for selecting numeric values"
license = "MIT"

[compatibility]
spec = ">=0.1.0"

[source]
origin = "@spec/components"
file = "slider.spec"
