[package]
name = "tabs"
version = "0.1.0"
category = "components"
description = "Tabbed interface for switching between related content panels"
license = "MIT"

[compatibility]
spec = ">=0.1.0"

[source]
origin = "@spec/components"
file = "tabs.spec"
