[package]
name = "drawer"
version = "0.3.0"
category = "components"
description = "Centred panel that slides in over a scrim, for navigation or forms"
license = "MIT"

[compatibility]
spec = ">=0.1.0"

[source]
origin = "@spec/components"
file = "drawer.spec"
