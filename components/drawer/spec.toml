[package]
name = "drawer"
version = "0.1.0"
category = "components"
description = "Slide-out panel anchored to a screen edge for navigation or forms"
license = "MIT"

[compatibility]
spec = ">=0.1.0"

[source]
origin = "@spec/components"
file = "drawer.spec"
