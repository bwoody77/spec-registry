[package]
name = "app-shell"
version = "0.1.0"
category = "components"
description = "Top-level layout wrapper with header, sidebar, and content regions"
license = "MIT"

[compatibility]
spec = ">=0.1.0"

[source]
origin = "@spec/components"
file = "app-shell.spec"
