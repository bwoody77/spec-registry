[package]
name = "bottom-sheet"
version = "0.1.0"
category = "components"
description = "Draggable sheet that slides up from the bottom of the screen"
license = "MIT"

[compatibility]
spec = ">=0.1.0"

[source]
origin = "@spec/components"
file = "bottom-sheet.spec"
