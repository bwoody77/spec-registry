[package]
name = "tree"
version = "0.1.0"
category = "components"
description = "Hierarchical tree view with expandable and collapsible nodes"
license = "MIT"

[compatibility]
spec = ">=0.1.0"

[source]
origin = "@spec/components"
file = "tree.spec"
