[package]
name = "accordion"
version = "0.1.0"
category = "components"
description = "Collapsible content sections with expand and collapse behavior"
license = "MIT"

[compatibility]
spec = ">=0.1.0"

[source]
origin = "@spec/components"
file = "accordion.spec"
