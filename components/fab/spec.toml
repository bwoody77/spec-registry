[package]
name = "fab"
version = "0.2.0"
category = "components"
description = "Floating action button for the primary screen action"
license = "MIT"

[compatibility]
spec = ">=0.1.0"

[source]
origin = "@spec/components"
file = "fab.spec"
