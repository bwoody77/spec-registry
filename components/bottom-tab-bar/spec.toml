[package]
name = "bottom-tab-bar"
version = "0.1.0"
category = "components"
description = "Fixed bottom navigation bar with icon and label tabs"
license = "MIT"

[compatibility]
spec = ">=0.1.0"

[source]
origin = "@spec/components"
file = "bottom-tab-bar.spec"
