[package]
name = "stack"
version = "0.1.0"
category = "components"
description = "Flexbox layout container for vertical or horizontal element spacing"
license = "MIT"

[compatibility]
spec = ">=0.1.0"

[source]
origin = "@spec/components"
file = "stack.spec"
