[package]
name = "avatar-picker"
version = "0.6.0"
category = "components"
description = "User avatar uploader with circular crop preview, zoom and pan"
license = "MIT"

[compatibility]
spec = ">=0.1.0"

[source]
origin = "@spec/components"
file = "avatar-picker.spec"

[extern]
files = ["../_extern/avatar-picker.js"]
