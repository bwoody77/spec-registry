[package]
name = "alert"
version = "0.3.0"
category = "components"
description = "Inline status notice — tinted, toned, optional title/icon/children, inset or full-bleed"
license = "MIT"

[compatibility]
spec = ">=0.1.0"

[source]
origin = "@spec/components"
file = "alert.spec"
