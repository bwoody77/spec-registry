[package]
name = "carousel"
version = "0.1.0"
category = "components"
description = "Horizontally scrollable slideshow for images or content panels"
license = "MIT"

[compatibility]
spec = ">=0.1.0"

[source]
origin = "@spec/components"
file = "carousel.spec"
