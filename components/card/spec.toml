[package]
name = "card"
version = "0.1.0"
category = "components"
description = "Contained surface for grouping related content and actions"
license = "MIT"

[compatibility]
spec = ">=0.1.0"

[source]
origin = "@spec/components"
file = "card.spec"
