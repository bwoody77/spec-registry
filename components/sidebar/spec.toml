[package]
name = "sidebar"
version = "0.1.0"
category = "components"
description = "Vertical navigation panel with collapsible sections and menu items"
license = "MIT"

[compatibility]
spec = ">=0.1.0"

[source]
origin = "@spec/components"
file = "sidebar.spec"
