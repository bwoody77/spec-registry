[package]
name = "snackbar"
version = "0.1.0"
category = "components"
description = "Brief message bar at the bottom of the screen with optional action"
license = "MIT"

[compatibility]
spec = ">=0.1.0"

[source]
origin = "@spec/components"
file = "snackbar.spec"
