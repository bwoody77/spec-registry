[package]
name = "stepper"
version = "0.1.0"
category = "components"
description = "Multi-step progress indicator for sequential workflows"
license = "MIT"

[compatibility]
spec = ">=0.1.0"

[source]
origin = "@spec/components"
file = "stepper.spec"
