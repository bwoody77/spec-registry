[package]
name = "toast"
version = "0.1.0"
category = "components"
description = "Temporary notification popup that auto-dismisses after a timeout"
license = "MIT"

[compatibility]
spec = ">=0.1.0"

[source]
origin = "@spec/components"
file = "toast.spec"
