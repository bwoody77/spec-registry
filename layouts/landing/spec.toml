[package]
name = "landing"
version = "0.1.0"
category = "layouts"
description = "Hero + sections marketing landing page with responsive full-width layout"
license = "MIT"

[compatibility]
spec = ">=0.1.0"

[source]
file = "landing.spec"
