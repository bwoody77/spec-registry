{
  "//": "Read by any repo's .claude/hooks/worktree-guard.mjs when an edit LANDS here — including edits reached from Vector through vector/.claude/worktrees/spec-registry, which is a symlink to this MAIN checkout.",
  "//why": "Every directory here is a PUBLISHED ARTIFACT written by `spec publish` from the spec monorepo — never hand-edited. Listing them as product code makes a hand edit fail loudly instead of silently diverging from the source that generated it. Fix the component in spec/packages/components/spec/ and republish.",
  "productPrefixes": [
    "a11y/",
    "animations/",
    "base/",
    "charts/",
    "components/",
    "connectors/",
    "dev-tools/",
    "fonts/",
    "i18n/",
    "icons/",
    "integrations/",
    "layouts/",
    "starters/",
    "themes/",
    "validators/"
  ]
}
